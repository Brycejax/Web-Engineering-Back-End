{
  "name": "y",
  "version": "0.0.1",
  "description": "This is the backend for the bookstore system",
  "main": "app.js",
  "scripts": {
    "start" : "nodemon app.js",
    "deploy" : "rsync -r exclude node_modules ./ username@aws-server-address:~/path/",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Alex Nordhoff",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.19.0",
    "express": "^4.17.1",
    "mongodb": "^3.3.2"
  },
  "devDependencies": {
    "nodemon": "^1.19.2"
  }
}
